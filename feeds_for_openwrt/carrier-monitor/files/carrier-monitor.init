#!/bin/sh /etc/rc.common

USE_PROCD=1
START=99

BIN=/usr/bin/carrier-monitor

start_service() {
	procd_open_instance
	procd_set_param command $BIN
	procd_set_param respawn 60 5 0
	procd_close_instance
}

